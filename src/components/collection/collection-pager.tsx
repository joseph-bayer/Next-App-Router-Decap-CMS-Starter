interface CollectionPagerProps {
  numberOfPages: number;
  onPageChange: (newPage: number) => void;
}

export default function CollectionPager({}: CollectionPagerProps) {
  return <div>TODO: CollectionPager!</div>;
}
