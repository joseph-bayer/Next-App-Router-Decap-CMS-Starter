import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About Us",
};

interface AboutPageProps {}

export default function AboutPage({}: AboutPageProps) {
  return <div>TODO: Implement About Page</div>;
}
